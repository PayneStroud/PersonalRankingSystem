# Web App Deployment

This web app is deployed from the `web/` folder via GitHub Pages workflow.

## First-time setup in GitHub

1. Open repo settings: `Settings -> Pages`
2. Under **Build and deployment**, set **Source** to `GitHub Actions`

After that, each push to `main` that changes `web/**` auto-deploys.
